<header title="Header">
    <nav class="menu-container flex items-center">
        <!-- menu-container-->
        <!-- Implementacion del menu Hamburguesa-->
        <input
            type="checkbox"
            aria-label="Toggle menu"
            class="menu-toggle"
            id="menu-toggle"
        />
        <!-- Lineas del menu -->
        <span></span>
        <span></span>
        <span></span>

        <!-- logo -->
        <a href="#" class="menu-logo">
            <!--menu-logo-->
            <img
                class="max-h-10 shrink-0"
                src="https://wweb.dev/resources/navigation-generator/logo-placeholder.png"
                alt="Logo"
            />
        </a>

        <!-- menu items -->
        <div class="menu">
            <!--menu-->
            <ul></ul>
            <ul>
                <li>
                    <a href="#1"> Inicio </a>
                </li>
                <li>
                    <a href="#2"> Servicios</a>
                </li>
                <li>
                    <a href="#"> Quienes Somos </a>
                </li>
                <li>
                    <a href="#"> Separa tu cita </a>
                </li>
            </ul>
        </div>
    </nav>
</header>

<style>
    .menu-container {
        background: rgba(218, 218, 218, 0.144);
        backdrop-filter: blur(4px);
        z-index: 1;
        -webkit-user-select: none;
        user-select: none;
        box-sizing: border-box;
        height: 70px;
        padding: 0 10%;
        position: absolute;
        justify-content: center;
    }

    .menu a {
        text-decoration: none;
        color: #777777;
        font-size: 20px;
        font-weight: 500; /* semibold */
        transition: color 0.3s ease;
        border-radius: 20px;
        padding: 4px 10px;
        
    }

    /* Animaciones de la burger */
    .menu-container input {
        display: block;
        width: 35px;
        height: 25px;
        margin: 0;
        position: absolute;
        cursor: pointer;
        opacity: 0;
        z-index: 2;
        -webkit-touch-callout: none;
    }

    /* Burger menu */
    .menu-container span {
        display: block;
        width: 33px;
        height: 4px;
        margin-bottom: 5px;
        position: relative;
        background: rgba(0, 0, 0, 0.4);
        border-radius: 3px;
        z-index: 1;
        transform-origin: 4px 0px;
        transition:
            transform 0.5s cubic-bezier(0.77, 0.2, 0.05, 1),
            background 0.5s cubic-bezier(0.77, 0.2, 0.05, 1),
            opacity 0.55s ease;
    }

    /* Animaciones de la Burger */
    .menu-container span:first-child {
        transform-origin: 0% 0%;
    }

    .menu-container span:nth-child(3) {
        transform-origin: 0% 100%;
    }

    .menu-container input:checked ~ span {
        opacity: 1;
        transform: rotate(45deg) translate(3px, -1px);
        background: #232323;
    }

    .menu-container input:checked ~ span:nth-child(4) {
        opacity: 0;
        transform: rotate(0deg) scale(0.2, 0.2);
    }

    .menu-container input:checked ~ span:nth-child(3) {
        transform: rotate(-45deg) translate(-5px, 11px);
    }

    /* mobile styles */

    @media only screen and (max-width: 818px) {
        .menu-container {
            flex-direction: column;
            align-items: flex-end;
            position: relative;
            z-index: 10; /* Asegúrate de que el menú esté sobre otros contenidos */
        }

        .menu-logo {
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            margin-left: 20px;
        }

        .menu-logo img {
            max-height: 35px;
        }

        .menu li {
            display: block;
            padding: 10px 0;
        }

        .menu {
            position: fixed; /* Cambiado de 'absolute' a 'fixed' para que el menú se expanda desde arriba */
            box-sizing: border-box;
            text-align: center;
            width: 100%;
            top: -500%; /* Inicialmente oculto arriba */
            left: 0;
            padding: 70px 0 30px;
            background: rgba(167, 166, 166, 1);
            backdrop-filter: blur(4px);
            -webkit-font-smoothing: antialiased;
            transition: top 0.5s cubic-bezier(0.77, 0.2, 0.05, 1);
            overflow-y: auto; /* Permite desplazamiento si el contenido es largo */
            height: auto; /* Asegura que el menú se expanda completamente */
        }

        .menu-container input:checked ~ .menu {
            top: 0; /* Cambia 'top' a 0 para mostrar el menú */
        }

        .menu li:hover {
            border-radius: 15px;
            background: rgba(167, 166, 166, 0.2);
        } d
    }

    /* desktop styles */
    @media only screen and (min-width: 818px) {
        .menu-container {
            width: 100%;
        }

        .menu ul {
            display: flex;
        }

        .menu li + li {
            margin-left: 10px;
        }
        .menu-container input {
            display: none;
        }

        /* Burger menu */
        .menu-container span {
            display: none;
        }

        .menu {
            position: relative;
            width: 100%;
            display: flex;
            justify-content: space-between;
        }
    }
</style>
