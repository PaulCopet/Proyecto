<footer class="">
    <div class="cont">
        <div class="contOlas">
            <svg
                class="olas"
                xmlns="http://www.w3.org/2000/svg"
                xmlns:xlink="http://www.w3.org/1999/xlink"
                viewBox="0 24 150 28"
                preserveAspectRatio="none"
                shape-rendering="auto"
            >
                <defs>
                    <path
                        id="gentle-ola"
                        d="M-160 44c30 0 58-18 88-18s 58 18 88 18 58-18 88-18 58 18 88 18 v44h-352z"
                    ></path>
                </defs>
                <g class="AnimOlas">
                    <use
                        xlink:href="#gentle-ola"
                        x="50"
                        y="0"
                        fill="rgba(246, 167, 0,0.3)"></use>
                    <use
                        xlink:href="#gentle-ola"
                        x="50"
                        y="3"
                        fill="rgba(246, 167, 0,0.5)"></use>
                    <use
                        xlink:href="#gentle-ola"
                        x="50"
                        y="5"
                        fill="rgba(246, 167, 0,.8)"></use>
                    <use xlink:href="#gentle-ola" x="50" y="7" fill="#f6a700"
                    ></use>
                </g>
            </svg>
        </div>
    </div>

    <div class="conPie pt-12 pb-2">
        <p> 2024 </p>

    </div>
</footer>

<style>
    footer {
        width: 100%;
        height: auto;
        padding-top: 5rem ;
        overflow: hidden;

        & .olas {
            position: absolute;
            width: 100%;
            height: 17vh;
            padding-bottom: -100%;
            min-height: 100px;
            max-height: 180px;
            transform: translateY(-100%);
        }

        /* Animación */

        & .AnimOlas > use {
            animation: move 25s cubic-bezier(0.55, 0.5, 0.45, 0.5) infinite;
        }

        & .AnimOlas > use:nth-child(1) {
            animation-delay: -2s;
            animation-duration: 7s;
        }

        & .AnimOlas > use:nth-child(2) {
            animation-delay: -3s;
            animation-duration: 10s;
        }

        & .AnimOlas > use:nth-child(3) {
            animation-delay: -4s;
            animation-duration: 13s;
        }

        & .AnimOlas > use:nth-child(4) {
            animation-delay: -5s;
            animation-duration: 20s;
        }

        p {
            text-align: center;
            font-size: 20px;
            height: 2rem;
        }

        .conPie {
            background-color: #f6a700;
            color: #fff;
            font-size: 1.5rem;
            font-weight: 700;
            display: flex;
            justify-content: center;
            align-items: center;
        }

    }

    @keyframes move {
        0% {
            transform: translate3d(-90px, 0, 0);
        }

        100% {
            transform: translate3d(90px, 0, 0);
        }
    }
</style>
